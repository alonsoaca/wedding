<% if object.errors.any? %>
  <div id="error_explanation">
    <h2>Hay errores que no permiten guardar la resrvación.</h2>
    <p>Hubo problemas con los siguientes campos:</p>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>